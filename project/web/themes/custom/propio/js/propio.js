/**
 * @file
 * Propio behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.propio = {
    attach: function (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
